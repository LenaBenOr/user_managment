apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
spec:
  containers:
  - name: nginx
    image: nginx
    volumeMounts:
    - name: secret-volume
      mountPath: "/tmp/secret-volume"
  volumes:
  - name: secret-volume
    secret:
      secretName: my-secret

